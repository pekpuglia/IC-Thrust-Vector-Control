module NozzleDraw

# function generate_nozzle_contour(internal_profile::Vector{Vector{Float64}})
    
# end

# profile_svg(noz::NozzleGeometry) = ConstructiveGeometry.svg("nozzle.svg", polygon(1000*generate_nozzle_contour(noz)))

# function generate_solid(profile::Vector)
#     return rotate_extrude() * ConstructiveGeometry.polygon(profile)
# end
    
end
